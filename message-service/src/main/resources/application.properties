# ================================================
# Message Service - Properties (Backup Config)
# Main configuration is in application.yml
# ================================================

spring.application.name=message-service

# This file is kept minimal as application.yml is the primary config
# Environment variables are defined in application.yml
